{% extends 'layout.html.twig' %}

{% block content %}
    <h1>Confirmar eliminación de {{ alumno }}</h1>
    <form method="post" action="{{ path('confirmar_borrar_alumnado', {'id': alumno.id}) }}" name="confirmar">
        <button type="submit" name="eliminar" class="btn btn-danger"><i class="fa fa-trash"></i> Sí, quiero eliminarlo</button>
        <a href="{{ path('listar_alumnado') }}" class="btn btn-info">No, quiero volver</a>
    </form>
{% endblock %}

